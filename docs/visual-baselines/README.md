# Визуальные базовые скриншоты пользовательского интерфейса

Эти изображения — текущий небольшой набор принятых состояний ключевого пользовательского интерфейса для ревью.

- `QAArtifacts/` остаётся временным доказательством graphical E2E, игнорируется Git и не коммитится.
- Базовые скриншоты — небольшой отобранный набор для ревью, а не копия всех `QAArtifacts`.
- Полный набор базовых скриншотов репозитория остаётся основным runtime-доказательством в источнике истины.
- `review/` при наличии может содержать облегчённые копии для инструментов и ревьюера; они не заменяют доказательство graphical-регрессии.
- Для удобного человеческого просмотра используется зеркало Google Drive `How I Fall/Визуальное ревью/`.
- `Текущие скриншоты/` содержит свежие изображения для ревью; `Референсы/` — внешние визуальные референсы; `Архив/` — необязательную историю.
- Копии на Drive не заменяют baselines репозитория, graphical E2E, тесты или CI.
- Предпросмотры для ревью создаются только для отобранных baseline-файлов, а не для каждого QA-скриншота.
- После значимого визуального прохода и успешного graphical E2E обновляются только релевантные baseline-файлы.
- Публикация UI + baseline-файлов может иметь статус `REVIEW CANDIDATE`, а не финальное визуальное одобрение.
- Ревьюер должен открывать реальные изображения и сравнивать их с предыдущим состоянием; при крупном обновлении полезны внешние game/VN references.
- Baselines не являются финальным артом, pixel-perfect golden-image tests или автоматическим эстетическим одобрением.

Имена PNG в репозитории остаются техническими идентификаторами и не переименовываются только ради перевода.

## Главное меню и настройки

- `main_menu.png` — art-first меню с крупным логотипом и короткой вертикальной типографической навигацией (TECH DEMO ONLY / NOT CANON: тестовая доступность).
- `main_menu_hover.png` — один активный пункт получает тонкий холодный маркер и едва заметный фон; уход мыши восстанавливает обычный вид.
- `main_menu_keyboard_focus.png` — отдельное состояние клавиатуры/контроллера; недоступное «Продолжить» приглушено без ложного маркера.
- `main_menu_quit_confirmation.png` — компактное подтверждение выхода с безопасным `Нет` по умолчанию и одним однозначным active/focus state.
- `main_menu_1280.png` — тот же композиционный приоритет и безопасные поля при 1280×720.
- `preferences.png` — крупная спокойная поверхность с горизонтальными категориями Экран / Звук / Текст / Игра, активной категорией, контекстной подсказкой и staged Apply; пять строк навигации Main Menu скрыты, пока Preferences открыт, поэтому типографика меню не читается сквозь полупрозрачную поверхность.
- `preferences_dropdown.png` — TMP dropdown режима экрана: все пункты видны без обрезания при 1920×1080.
- `preferences_sound_dirty.png` — категория Звук: лёгкие sliders, читаемые значения, строка фокуса с контекстной подсказкой, неприменённое изменение и доступный Apply.
- `preferences_dropdown_1280.png` — dropdown при 1280×720: controls, contextual hint и footer остаются читаемыми и не перекрываются.
- `preferences_windowed_dropdown.png` — dropdown разрешения в режиме «Окно» на 1920×1080 desktop: показываются только значения, которые помещаются на рабочий стол (1280×720, 1600×900); черновик видимо нормализован в 1600×900 до Apply, без обрезания пунктов.
- `preferences_applied_disabled.png` — то же окно после Apply: кнопка «Применить» disabled и её подпись явно приглушена (тёмный серо-синий оттенок вместо почти белого enabled-текста), при этом enabled/dirty-состояние остаётся ярко-белым.

Main Menu и Preferences-набор обновлён по полному PlayerUi graphical E2E от 2026-09-20: 69 свежих снимков, `playerPrefsRestored=true`; набор включает исправление иерархии — при открытии Preferences из Main Menu пять строк действий меню скрываются и возвращаются при закрытии, фон и полупрозрачность не изменились. Дополнительные состояния Text, after Apply, slider focus и gameplay parity остаются в ignored QAArtifacts. Это REVIEW CANDIDATE, не субъективное visual approval. Micro-polish pass 2026-09-21: `preferences_sound_dirty.png` переснят, добавлен `preferences_applied_disabled.png` (disabled-подпись Apply приглушена явно, layout и логика dirty/applied не менялись).

Main Menu target v1 pass 2026-09-22: навигация приведена к утверждённому `01_Main_Menu_Target_v1.png` — левая колонка сдвинута к краю (текст ≈ x142 при 1920×1080), ровный плотный ритм строк 380×76 с шагом 84, шрифт 42; активная строка получает яркую cyan-полосу во всю высоту строки (#02D9F9) и глубокую сине-стеклянную плашку; nav-side wash стал настоящим navy-градиентом (#0C1422, пиковая альфа ≈0.86, выцветает к ~46% ширины) вместо почти невидимого authored-градиента; логотип посажен в левый верхний угол (видимые штрихи ≈ 98..488 × 54..388) с учётом прозрачных полей спрайта; внизу слева добавлен tagline `SAME HALLS / DIFFERENT YOU` с cyan-тире. semantics hover/focus/disabled и все маршруты не менялись. Пересняты `main_menu.png`, `main_menu_hover.png`, `main_menu_keyboard_focus.png`, `main_menu_quit_confirmation.png`, `main_menu_1280.png` по полному PlayerUi graphical E2E (`playerPrefsRestored=true`). Это REVIEW CANDIDATE против target v1, не субъективное visual approval.

Reviewer correction на том же pass: tagline и cyan-тире включены в существующий Main Menu → Preferences hide/show lifecycle (тот же контракт, что скрывает пять action rows) — под полупрозрачной Preferences surface больше нет `SAME HALLS / DIFFERENT YOU` ghost; при закрытии оба элемента возвращаются вместе со строками. `preferences.png` переснят по свежему PlayerUi graphical E2E; остальные состояния Main Menu попиксельно не изменились.

Background replacement pass 2026-09-23 (PR #18): фон Main Menu заменён на утверждённый целевой имидж Cinematic Campus Duality (`main_menu_background.png`, содержимое перезаписано in-place, GUID и сцена не менялись). Пересняты по fresh полному PlayerUi graphical E2E (`status=PASS`, `playerPrefsRestored=true`, все 69 обязательных кадров): `main_menu.png`, `main_menu_hover.png`, `main_menu_keyboard_focus.png`, `main_menu_quit_confirmation.png`, `main_menu_1280.png`. Композиция target v1, nav-side wash, типографика, семантика hover/focus/disabled и все маршруты не менялись — во всех состояниях изменился только изображаемый фон; навигация остаётся читаемой, confirmations, Load-панель и Preferences-surface структурно валидны, обрезок/растяжений/перекрытий нет. Прочие Main Menu-состояния (preferences, load, pointer exit, quit yes-focus) проверены по тем же кадрам E2E — дефектов не выявлено; отдельных baseline-файлов для них не существует, набор остаётся прежним. Это REVIEW CANDIDATE, не субъективное visual approval.

Target v1 correction pass 2026-09-23 (hands-on): левый nav-side wash доведён до силы утверждённого `01_Main_Menu_Target_v1.png` — исправлена гамма-ловушка (runtime-градиент хранил sRGB-байты в linear-текстуре и отображался светлым серо-синим; теперь navy #101A28 предконвертирован pow(2.2), как в Game Menu), пиковая альфа поднята до ≈0.94, ширина панели увеличена с 880 до 1040px при 1920×1080 (выцветает к ~54% ширины). Активная строка (hover/фокус) переведена с плоской прямоугольной плашки с жёстким правым краем на runtime-рампу: бирюзово-синяя светимость (#1C789E) максимальна у левого края строки и линейно растворяется к правому, жёсткой границы нет; cyan-бар #02D9F9 и семантика hover/focus/disabled/pressed не менялись. Типографика, композиция, логотип, tagline и все маршруты соответствовали цели и не менялись. Пересняты по fresh полному PlayerUi graphical E2E (`status=PASS`, `playerPrefsRestored=true`): `main_menu.png`, `main_menu_hover.png`, `main_menu_keyboard_focus.png`, `main_menu_quit_confirmation.png`, `main_menu_1280.png`. Это REVIEW CANDIDATE, не субъективное visual approval.

## Сохранение и загрузка

Единая полоса навигации Save/Load: в Load — `QS | AS | 1..10`, в Save — только `1..10`. `QS` — 6 ротируемых Quick-слотов, `AS` — 6 Auto-слотов, число — страница ручных слотов (глобальный слот = (страница − 1) × 6 + локальный слот). Отдельного ряда Manual/Auto/Quick и стрелок страниц больше нет; активна ровно одна запись полосы. Запись в Quick/Auto через Save невозможна.

- `save_load_manual.png` — Load, Manual страница 1: полоса `QS AS 1..10` с активной страницей 1, валидный слот и пустые карточки.
- `save_load_quick.png` — Load, `QS` активна: шесть ротируемых Quick-слотов; состояние `AS`/Auto покрывается свежим graphical E2E.
- `save_load_save.png` — Save после успешной записи: полоса только `1..10`, тост «Слот 1 сохранён» в собственной полосе над навигацией без перекрытия страниц.
- `save_load_confirmation.png` — подтверждение загрузки поверх новой полосы: безопасный Cancel default, родительский контент заблокирован dimmer-слоем.
- `save_load_delete_confirmation.png` — destructive-вариант той же лёгкой modal-композиции: действие различимо без большой красной кнопки.
- `save_load_slot_types.png` — занятый invalid-slot визуально отличим от пустого empty-slot; тост «Сохранение недоступно» больше не перекрывает полосу навигации.
- `save_load_manual_page_2.png` — Load, страница 2 при 1280×720: сводка показывает глобальный СЛОТ 07, полоса без обрезок и перекрытий.
- `save_load_save_1280.png` — Save при 1280×720: полоса `1..10` и сетка 3×2 без перекрытий.
- `save_load_filled_slot.png` — Save с заполненным слотом 1 при 1920×1080: номер слота, дата и нижняя metadata-строка заметно читаемее, дата чуть контрастнее прежнего серого; grid-геометрия и число карточек не менялись.
- `save_load_filled_slot_1280.png` — то же состояние при 1280×720: pagination, дата карточки и metadata остаются читаемыми без увеличения экрана или сетки.

Save/Load-набор обновлён после unified strip graphical E2E от 2026-09-18 (SaveBackendV2 + ManualSave + PlayerUi). Это REVIEW CANDIDATE, не субъективное visual approval.

## Игровое меню

UX-контракт отката после прохода 2026-09-21: откат доступен только колёсиком мыши и кнопкой «Назад» в нижнем Quick Menu; боковое игровое меню содержит исключительно системные действия (`Сохранить / Загрузить / Настройки / Главное меню / Выйти` и «Вернуться в игру»).

- `game_menu_root.png` — UI Target v1: цельная полно-высотная левая navy glass-панель поверх яркой, читаемой сцены (лёгкий scrim вместо тяжёлого затемнения); блок бренда «HOW I FALL» с cyan-тире и tagline сверху, спокойные строки навигации с шевронами, разделитель и интегрированная «Вернуться в игру» (default focus — cyan-полоса и лёгкая плашка) над нижним tagline; красного selection/focus в меню больше нет.
- `reading_game_menu_1280.png` — та же композиция при 1280×720: пропорции панели, строки и tagline масштабируются когерентно, без обрезок.
- `game_menu_main_confirmation.png` — подтверждение возврата в главное меню: экран изолирован dimmer-слоем, безопасное `Нет` получает default focus.
- `game_menu_embedded_load.png` — встроенный Load: полоса `QS AS 1..10`, сетка 3×2 и активный cyan-маркер на «Загрузить» на сценовой стороне панели, без пересечения с навигацией.
- `game_menu_embedded_save_1280.png` — встроенный Save при 1280×720: сетка и пагинация помещаются без обрезок.
- `game_menu_save_load_return_focus.png` — возврат из встроенного Save/Load: дефолт-фокус «Вернуться» восстановлен ровно с одним cyan-маркером.

Game Menu target parity pass 2026-09-22: панель приведена к `03_Game_Menu_Target_v1.png` — три параллельные плиты (wash + Header + Navigation) заменены одной полно-высотной стеклянной колонной (~26% ширины, runtime navy-градиент с предварительным linear-преобразованием цвета), полноэкранное затемнение снижено до лёгкого art-first scrim, тяжёлые кнопочные карточки стали тихими строками с шевронами и пропорциональным левым отступом, фокус-маркер переключён с красного правого на cyan #02D9F9 левый (язык Main Menu target v1), «Вернуться в игру» интегрирована в композицию с ▶-иконкой под разделителем, добавлены блок бренда (типографический wordmark вместо NOT CANON логотипа) и tagline сверху/снизу панели. Семантика действий, Esc-стек, Save/Load lifecycle, Replay-видимость и владение Quick Menu не менялись. Пересняты по fresh GameMenu + PlayerUi graphical E2E (`playerPrefsRestored=true`): все шесть файлов выше. Верх панели над ярким окном сцены слегка подсвечен тёплым grade/bloom сцены — то же свойство рендера было и в предыдущем принятом baseline (там сильнее). Это REVIEW CANDIDATE против target v1, не субъективное visual approval.

## Основной опыт чтения

- `reading_standard.png` — центрированное floating-чтение: текст лежит поверх сцены, поле чтения выровнено по горизонтальной оси, текст внутри поля остаётся выключен влево, а вместо постоянной карточки остаётся только мягкий локальный градиент и низковесное `Назад / История / Пропуск / Авто / Быстр. сох.` по нижнему центру.
- `reading_dialogue_125.png` — длинный диалог при поддерживаемом масштабе текста 125%: все строки внутри поля, без обрезания текста и перекрытия Quick Menu.
- `reading_named_speaker_1280.png` — имя говорящего типографически связано с репликой без отдельной плашки и остаётся читаемым при 1280×720.
- `reading_choice_two.png` — состояние с 2 вариантами и детерминированным первым фокусом клавиатуры/контроллера.
- `reading_choice_four_long.png` — 4 видимых варианта и длинный переносимый вариант ровно на четыре строки с равными вертикальными отступами карточки, без ellipsis, перекрытий и потери текста.
- `reading_choice_four_long_1280.png` — те же четыре варианта при 1280×720: длинная карточка сохраняет вертикальный воздух и остаётся внутри экрана, не пересекая диалог и Quick Menu.
- `reading_choice_hover.png` — наведение мышью переносит фокус выбора без второго одновременно выбранного состояния.
- `reading_relationship_cue_positive.png` — невербальный индикатор положительного последствия.
- `reading_relationship_cue_negative.png` — невербальный индикатор отрицательного последствия.
- `reading_relationship_cue_mixed.png` — невербальный индикатор смешанного последствия.
- `reading_after_relationship_cue.png` — обычное чтение после очистки индикатора последствия.
- `reading_backlog.png` — полноразмерная История с визуально разделёнными репликами, акцентом актуального блока и читаемым скроллом.
- `reading_backlog_1280.png` — адаптивная История без наложения заголовка, текста, скролла и кнопки закрытия.
- `reading_auto_active.png` — заметное, но спокойное активное состояние Auto с акцентной подписью.
- `reading_quick_menu_hover.png` — hover на «История»: плашка заметно светлее обычного состояния, но слабее teal ACTIVE Auto/Skip (контракт hover < ACTIVE); полоса теперь из пяти действий с «Назад» слева.
- `reading_quick_menu_rollback.png` — нижняя полоса чтения с добавленной rollback-кнопкой «Назад» слева от `История / Пропуск / Авто / Быстр. сохр.`: полоса остаётся одной компактной нижне-центральной строкой, ширина следует за подписями кнопок.
- `reading_quick_menu_rollback_1280.png` — та же полоса при 1280×720 без потери компактности и читаемости подписей.
- `reading_skip_active.png` — заметное, но спокойное активное состояние Skip с акцентной подписью.
- `reading_quick_save_feedback.png` — краткий отклик быстрого сохранения поверх обычной поверхности чтения.

Reading-набор обновлён по полному PlayerUi graphical E2E от 2026-09-18: поле чтения центрировано по горизонтали как композиция (текст внутри остаётся выровнен влево), мягкий scrim стал горизонтально симметричным с чуть более тёмной полезной серединой против яркой сцены, Quick Menu переехало в компактный нижне-центральный ряд, а тень/обводка текста и типографическая привязка имени сохранены. Действия Quick Menu и Choice UI не менялись. Дополнительные состояния 125%-диалога и choices при 1280×720 остаются в ignored QAArtifacts. Это REVIEW CANDIDATE, не субъективное visual approval. Micro-polish pass 2026-09-21: `reading_standard.png`, `reading_auto_active.png` пересняты (подписи Quick Menu укрупнены умеренно, strip остался компактным), добавлены `reading_quick_menu_hover.png` и `reading_choice_four_long_1280.png`, `reading_choice_four_long.png` переснят с четырёхстрочным вариантом и увеличенным вертикальным паддингом карточки.

Reading target parity pass 2026-09-22: поверхность чтения приведена к утверждённому `02_Reading_Target_v1.png` — имя говорящего стало насыщенным cyan-акцентом и сидит вровень с левым краем диалоговой колонки, под именем добавлено короткое cyan-тире с бледнеющим хвостом (namepost-иерархия цели); нижняя полоса чтения переведена с отдельных плашек-чипов на плоский язык target: тихие подписи 17px, тонкие cyan-разделители между действиями и одна общая мягкая navy-полоса-подложка; hover/фокус получил cyan-подчёркивание действия (лестница rest < selected < hover < pressed), ACTIVE Auto/Skip — постоянное более яркое cyan-подчёркивание с brighter-подписью (контракт hover < ACTIVE сохранён); индикатор advances переведён на cyan. Сгенерированный персонаж/фон target не импортировались (TECH DEMO ONLY / NOT CANON); семантика typewriter, advance, rollback, Hide UI, Text Size (включая 125%), textbox opacity, Auto/Skip, choices и Quick Menu actions не менялась. Пересняты по fresh PlayerUi graphical E2E (`playerPrefsRestored=true`): `reading_standard.png`, `reading_dialogue_125.png`, `reading_named_speaker_1280.png`, `reading_quick_menu_rollback.png`, `reading_quick_menu_rollback_1280.png`, `reading_quick_menu_hover.png`, `reading_auto_active.png`, `reading_skip_active.png`, `reading_quick_save_feedback.png`, `reading_choice_two.png`, `reading_choice_hover.png`, `reading_choice_four_long.png`, `reading_choice_four_long_1280.png`, `reading_after_relationship_cue.png`; `reading_backlog.png` и `reading_backlog_1280.png` попиксельно не изменились. `reading_relationship_cue_positive/negative/mixed.png` остаются легаси-снимками видимого cue (текущий demo-shell контракт подавляет глиф, раннер это состояние не воспроизводит) и в этом pass не обновлялись. Это REVIEW CANDIDATE против target v1, не субъективное visual approval.

Core UI baseline-набор обновлён после двух визуальных итераций Main Menu, Preferences и Save/Load и финальных проходов `PlayerUi`, `ManualSave` и `SaveBackendV2` от 2026-09-15. Choices не перерабатывались: benchmark не дал наблюдаемой поверхности выбора, поэтому сохранены существующие HIF-контракты и baseline-состояния.
