interface:
  display_name: "How I Fall Visual QA"
  short_description: "Run focused graphical QA at 1920x1080"
  default_prompt: "Use $hif-visual-qa to run focused How I Fall graphical QA using existing launchers/E2E, inspect actual screenshot proof, and report only checks that were really performed."
policy:
  allow_implicit_invocation: true
